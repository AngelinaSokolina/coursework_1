[tool.poetry]
name = "coursework-1"
version = "0.1.0"
description = ""
authors = ["Angelina <akusilda@proton.me>"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.14"
pandas = "^3.0.3"
openpyxl = "^3.1.5"
pytest = "^9.0.3"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
[dependency-groups]
dev = [
    "pytest-cov (>=7.1.0,<8.0.0)"
]
